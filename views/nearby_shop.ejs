<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nearby Shops</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 flex justify-center items-center min-h-screen">
    <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-lg">
      <h1 class="text-2xl font-bold text-gray-800 mb-6 text-center">
        Nearby Shops
      </h1>

      <!-- Address Input Form -->
      <form id="address-form" action="/shops" class="mb-6">
        <label for="address" class="block text-gray-700 font-semibold mb-2"
          >Enter Your Address:</label
        >
        <input
          type="text"
          id="address"
          name="address"
          class="w-full p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          placeholder="Enter your address"
        />
        <button
          type="submit"
          class="mt-4 w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-lg shadow-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
        >
          Find Nearby Shops
        </button>
      </form>

      <!-- Shop List -->
      <ul id="shop-list" class="space-y-4 hidden">
        <!-- Example Shop List (Dynamic Content) -->
        <!-- The shops will be displayed here based on the entered address -->
      </ul>

      <p id="instructions" class="mt-6 text-center hidden">
        Select a shop and follow the instructions to submit your device.
      </p>
    </div>

   <script src="/javascript/nearby.js"></script>
  </body>
</html>
